<UserControl x:Class="EtherChess.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#262421">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Stats -->
            <RowDefinition Height="Auto"/> <!-- Actions -->
            <RowDefinition Height="*"/>    <!-- History -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/> <!-- Right Sidebar -->
        </Grid.ColumnDefinitions>

        <!-- Main Content Area -->
        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,20">
            <!-- Rating Card -->
            <Border Background="#2D2B29" CornerRadius="8" Padding="20" Margin="0,0,20,0" Width="200">
                <StackPanel>
                    <TextBlock Text="Play" Foreground="White" FontSize="18" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock Text="🏆" FontSize="32" Margin="0,0,10,0"/>
                        <StackPanel>
                            <TextBlock Text="{Binding Rating}" Foreground="White" FontSize="32" FontWeight="Bold"/>
                            <TextBlock Text="Rating" Foreground="#888" FontSize="14"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Puzzles Card -->
            <Border Background="#2D2B29" CornerRadius="8" Padding="20" Width="200">
                <StackPanel>
                    <TextBlock Text="Puzzles" Foreground="White" FontSize="18" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock Text="🧩" FontSize="32" Margin="0,0,10,0"/>
                        <StackPanel>
                            <TextBlock Text="{Binding PuzzlesSolved}" Foreground="White" FontSize="32" FontWeight="Bold"/>
                            <TextBlock Text="Solved" Foreground="#888" FontSize="14"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Actions -->
        <Grid Grid.Row="1" Grid.Column="0" Margin="0,0,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Command="{Binding PlayVsAICommand}" Margin="0,0,10,0" Height="100" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#2D2B29" CornerRadius="8" Padding="15">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="⏱" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,5" Foreground="White"/>
                                <TextBlock Text="Play vs AI" Foreground="White" FontWeight="SemiBold"/>
                                <ComboBox ItemsSource="{Binding Difficulties}" SelectedItem="{Binding SelectedDifficulty}" Margin="0,10,0,0" Width="100"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Command="{Binding PlayMultiplayerCommand}" Grid.Column="1" Margin="5,0,5,0" Height="100" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#2D2B29" CornerRadius="8" Padding="15">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="♟️" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                <TextBlock Text="New Game" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Grid.Column="2" Margin="10,0,0,0" Height="100" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#81B64C" CornerRadius="8" Padding="15">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="🧩" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                <TextBlock Text="Solve Puzzle" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>

        <!-- Game History -->
        <Border Grid.Row="2" Grid.Column="0" Background="#2D2B29" CornerRadius="8" Padding="20" Margin="0,0,20,0">
            <StackPanel>
                <TextBlock Text="Game History" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                
                <TextBlock Text="No recent games found." Foreground="#888" FontStyle="Italic" 
                           Visibility="{Binding RecentGames.Count, Converter={StaticResource CountToVisibilityConverter}, FallbackValue=Visible}"/>

                <ItemsControl ItemsSource="{Binding RecentGames}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Ellipse Width="30" Height="30" Fill="#444" Margin="0,0,10,0"/>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Opponent}" Foreground="White" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Rating}" Foreground="#888" FontSize="12"/>
                                </StackPanel>
                                <TextBlock Grid.Column="2" Text="{Binding Result}" Foreground="White" VerticalAlignment="Center" Margin="0,0,20,0"/>
                                <Button Content="Review" Background="#444" Foreground="White" BorderThickness="0" Padding="10,5" Grid.Column="3"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- Right Sidebar (Stats Only) -->
        <StackPanel Grid.Row="0" Grid.RowSpan="3" Grid.Column="1">
            <Border Background="#2D2B29" CornerRadius="8" Padding="20">
                <StackPanel>
                    <TextBlock Text="Your Stats" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Games" Foreground="#888" Width="100"/>
                        <TextBlock Text="{Binding RecentGames.Count}" Foreground="White" FontWeight="Bold"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Puzzles" Foreground="#888" Width="100"/>
                        <TextBlock Text="{Binding PuzzlesSolved}" Foreground="White" FontWeight="Bold"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Win Rate" Foreground="#888" Width="100"/>
                        <TextBlock Text="{Binding WinRate}" Foreground="White" FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>

    </Grid>
</UserControl>
