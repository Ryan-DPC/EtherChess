<Window x:Class="EtherChess.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:EtherChess.ViewModels"
        xmlns:v="clr-namespace:EtherChess.Views"
        mc:Ignorable="d"
        Title="Ether Chess" Height="720" Width="1280"
        Background="#262421"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <v:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:GameViewModel}">
            <v:GameView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/> <!-- Sidebar -->
            <ColumnDefinition Width="*"/>   <!-- Content -->
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Background="#211F1D" BorderBrush="#333" BorderThickness="0,0,1,0">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Logo -->
                    <RowDefinition Height="Auto"/> <!-- Profile -->
                    <RowDefinition Height="*"/>    <!-- Nav -->
                    <RowDefinition Height="Auto"/> <!-- Bottom -->
                </Grid.RowDefinitions>

                <!-- Logo -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                    <TextBlock Text="♟️" FontSize="24" Foreground="#81B64C" Margin="0,0,10,0"/>
                    <TextBlock Text="EtherChess" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Profile -->
                <Border Grid.Row="1" Background="#2D2B29" CornerRadius="8" Padding="15" Margin="0,0,0,30">
                    <StackPanel>
                        <Ellipse Width="60" Height="60" Fill="#888" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding Username}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" FontSize="16"/>
                    </StackPanel>
                </Border>

                <!-- Navigation -->
                <StackPanel Grid.Row="2">
                    <Button Command="{Binding NavigateToDashboardCommand}" Margin="0,0,0,10" HorizontalContentAlignment="Left" Background="Transparent" BorderThickness="0" Foreground="White" Padding="10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🏠" Margin="0,0,15,0"/>
                            <TextBlock Text="Dashboard"/>
                        </StackPanel>
                    </Button>
                    <Button Margin="0,0,0,10" HorizontalContentAlignment="Left" Background="Transparent" BorderThickness="0" Foreground="#888" Padding="10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🧩" Margin="0,0,15,0"/>
                            <TextBlock Text="Puzzles"/>
                        </StackPanel>
                    </Button>
                    <Button Margin="0,0,0,10" HorizontalContentAlignment="Left" Background="Transparent" BorderThickness="0" Foreground="#888" Padding="10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📚" Margin="0,0,15,0"/>
                            <TextBlock Text="Learn"/>
                        </StackPanel>
                    </Button>
                    <Button Margin="0,0,0,10" HorizontalContentAlignment="Left" Background="Transparent" BorderThickness="0" Foreground="#888" Padding="10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👥" Margin="0,0,15,0"/>
                            <TextBlock Text="Community"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Settings -->
                <StackPanel Grid.Row="3">
                    <TextBlock Text="⚙️ Settings" Foreground="#888" Padding="10"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <ContentControl Grid.Column="1" Content="{Binding CurrentView}"/>
    </Grid>
</Window>
